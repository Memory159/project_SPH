import { reqGetCode } from "@/api";
// 登录与注册的模块
const state = {};
const mutations = {};
const actions = {
    // 获取验证码
    reqGetCode({ commit }, phone) {

    }
};
const getters = {};

export default {
    state,
    mutations,
    actions,
    getters,
}